stages:
  - deploy

netlify:
  stage: deploy
  image: node:10.15.3
  script:
  - npm install
  - npm install -g yarn netlify-cli
  - yarn build
  - netlify --telemetry-disable
  - netlify deploy --dir=./build --auth=$NETLIFY_TOKEN --prod
  artifacts:
    paths:
    - public
  only:
  - master